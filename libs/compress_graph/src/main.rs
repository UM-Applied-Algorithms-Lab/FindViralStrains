// Take graph, sources, and sinks, sort each of them, then go through and find the amount
// of connections for each node. If the total connections for a node is more than two,
// skip to the next one, otherwise compress (no need to find all connection, just 3)


fn main() {
    println!("Hello, world!");
}
